[project]
name = "if-pplant-geostorage"
version = "0.1.0"
description = "Interface for coupled simulation of power plant and geological storage."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "tespy==0.9.15",
]
