language: python

matrix:
  include:
    - python: 3.12
    - python: 3.13
      dist: jammy
      sudo: true

install:
  - pip install -e .
  - pip install pytest pytest-cov coveralls

# command to run tests
script:
  - pytest --doctest-modules --cov=coupled_simulation

after_success:
- coveralls
